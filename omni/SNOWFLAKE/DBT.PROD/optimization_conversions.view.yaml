# Reference this view as dbt_prod__optimization_conversions
catalog: DBT
schema: PROD
table_name: OPTIMIZATION_CONVERSIONS

dimensions:
  account:
    sql: '"ACCOUNT"'

  platform:
    sql: '"PLATFORM"'

  optimization_conversion:
    sql: '"OPTIMIZATION_CONVERSION"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: optimization_conversions
  target_schema: PROD
  config:
    materialized: seed
  referenced_by:
    [
      facebook_ads_performance,
      google_ads_adgroup_performance,
      google_ads_keyword_performance,
      google_ads_campaign_performance
    ]
